How to Set Things Up and Run the Server
=======================================

```
pip install -r requirements.txt
python config.py
python demo.py
```

The server will start on localhost on port 5000.
